// Genesis logic has been folded into chain.rs (Ledger::init / create_genesis).
// This module is kept as a placeholder so that `mod genesis;` in mod.rs
// doesn't break. It will be removed once all callers are migrated.
